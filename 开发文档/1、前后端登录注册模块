# 用户认证模块企业化修订计划

## 审查结论

现有方案的分层、P0/P1/P2 优先级、验证码填写方式、敏感配置外置等方向正确；但尚不满足企业生产上线基线。主要缺口是：账户状态、验证码安全、会话撤销、限流审计、隐私合规与生产数据库。

密码存储应优先使用 Argon2id；bcrypt 可作为兼容选择。登录及验证码接口必须有账户与 IP 维度的限流，并避免通过错误信息枚举账户。参考 [OWASP 密码存储建议](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)、[认证防护建议](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)。

## 关键设计调整

- P0 注册强制要求邮箱，修正 `users.email` “可为空”与“未验证不得登录”的冲突；`username`、`email` 建立数据库唯一约束和规范化比较策略。
- 将 `is_verified` 升级为账户状态：`pending_verification / active / disabled`；补充 `created_at`、`updated_at`。所有受保护接口通过统一 `get_current_user` 校验用户存在且状态为 `active`。
- 验证码表不存明文 `code`，改为带服务端密钥的摘要，并增加 `expires_at`、`consumed_at`、`attempt_count`、`last_sent_at`。新验证码使旧记录失效；验证成功、过期、超过尝试次数均不可复用。
- P0 使用同域 Web：登录成功后设置短期 access JWT（15 分钟）和轮换 refresh token（7 天），均存入 `Secure + HttpOnly + SameSite` Cookie；增加 `/refresh`、`/logout`、`auth_sessions`。JWT 固定算法白名单并校验 `sub`、`exp`、`iss`、`aud`、`jti`，支持密钥轮换。JWT 的算法、签发方和受众校验符合 [RFC 8725](https://www.rfc-editor.org/info/rfc8725/)。
- Cookie 认证下，所有写操作启用 CSRF 防护；生产 CORS 仅允许明确的前端源，强制 HTTPS。
- 对注册、登录、验证、重发均做 Redis 分布式限流：按 IP、账号/邮箱分别限制；登录采用渐进式延迟或短时锁定，记录异常事件。公开登录响应统一为“账号或密码错误，或账号尚未激活”，注册完成页承担“请验证邮箱”的明确引导。
- SMTP 个人邮箱仅用于开发验证；生产采用企业邮件服务、专用发信域名和 SPF/DKIM/DMARC。邮件通过 outbox/后台任务发送并记录投递失败，不在请求中暴露 SMTP 异常。
- 生产环境迁移至 PostgreSQL + Alembic；SQLite 只保留本地开发与教学。日志记录认证成功/失败、验证码重发/验证和会话撤销，但不得记录密码、JWT、验证码或完整邮箱/手机号。
- P1 不能视为“零额外成本”：手机号成为登录名之前必须证明号码归属，因此应与短信验证能力一起上线，或暂缓手机号登录。P2 的短信码同样不存明文，并限制手机号、IP、设备及每日发送量。
- 在注册页提供隐私政策与必要告知，说明邮箱/手机号的用途、保存期限和用户权利；收集范围遵循最小必要原则。参考 [《个人信息保护法》第六、十七、十九条](https://www.npc.gov.cn/WZWSREL25wYy9jMi9jMzA4MzQvMjAyMTA4L3QyMDIxMDgyMF8zMTMwODguaHRtbD9yZWY9aW1i)。

## 接口与验收

- P0 保留 `register / login / me / verify-email / resend-verification`，新增 `refresh / logout`；注册、重发使用不泄露账户存在性的响应。
- 覆盖：重复注册并发冲突、验证码过期/错误/超限/重发失效、未激活账户无法取得会话、禁用账户的旧会话立即失效、JWT 过期/错误算法/错误签发方或受众、跨站写请求 CSRF 拒绝、限流触发、日志不含敏感值。
- 发布前执行数据库迁移演练、备份恢复演练、SMTP 失败重试测试、依赖与密钥扫描，并确认 `.env` 永不提交。

## 已确定默认项

- 第一版为同域 Web。
- P0 邮箱必填，验证前账户不可用。
- 不采用 24 小时单 JWT；采用短 access token + 可撤销、轮换 refresh token。
- 密码找回已使用独立 `password_resets` 表和专用邮件验证码实现；MFA 的数据结构和接口保留，但当前产品暂时关闭 MFA，不能将其与图片验证码混为同一种安全能力。

## 实施变更记录（2026-08-03）

### 邮箱验证码邮件

- 已实现 SMTP HTML 邮件模板：发件人显示为“启程学堂”，邮件主题包含验证码，同时保留纯文本版本以兼容不支持 HTML 的邮件客户端。
- 模板沿用前端学习平台的纸白、薄荷绿和深绿色调，突出显示 6 位验证码，并明确验证码 15 分钟有效、不得泄露。
- 注册或重发验证码接口返回 `202 Accepted` 时，表示验证码已进入 outbox 投递流程；实际投递失败会由后台任务标记为 `failed`，不在接口响应中暴露 SMTP 细节。
- `.env.example` 仅保留 SMTP 配置占位符。真实邮箱地址和 SMTP 授权码只能存放在本机 `.env`，不得提交；若授权码曾出现在仓库文件中，应立即在邮箱服务商后台轮换。

### 注册密码规则

- 注册密码统一为：至少 12 位，且必须同时包含英文字母（`A-Z`/`a-z`）、数字（`0-9`）和特殊符号。
- 前端在输入时显示固定规则、强度状态和明确的未达标提示；点击“发送验证码”前会拦截不符合规则的密码。
- 后端 `RegisterRequest` 使用相同规则再次校验，防止绕过前端直接调用注册接口。登录接口不重复校验密码复杂度，只负责认证，避免影响已有账户登录。
- 已补充后端测试用例：缺少字母、数字、特殊符号或少于 12 位的密码必须拒绝；符合规则的密码可通过。

### 前端认证错误提示

- FastAPI 的 `422` 校验错误可能以 `detail` 对象数组返回；前端已统一转换为可读文本，例如“请填写邮箱”“验证码格式不正确”“密码至少需要 12 个字符”，不再显示 `[object Object]`。
- 页面初始化请求 `/api/auth/csrf` 返回 `200`，随后未登录状态请求 `/api/auth/me` 返回 `401` 属于正常会话检查，不代表系统异常。

### 验证状态

- 前端生产构建已通过。
- 后端自动化测试使用临时 SQLite 数据库和内存邮件发送器，与本机 `.env` 中的真实 SMTP 配置隔离；可安全执行 `pytest`，不触发真实邮件投递。
- 邮件投递失败会在服务端日志记录 outbox 编号、SMTP 错误类型和已脱敏的错误原因；不记录验证码、授权码或完整邮箱。数据库仍只保存通用状态 `delivery_failed`，避免向接口或数据表泄露邮件服务细节。
- 修复邮件模板构造函数未返回 `EmailMessage` 对象的问题；该问题会导致 SMTP 在发送阶段报出 `AttributeError: 'NoneType' object has no attribute 'get_all'`。已补充邮件对象类型断言，防止同类回归。
- 修复用户名登录的 SQLAlchemy 条件构造：仅输入用户名时，查询不再向 `or_()` 传入空字符串；用户名和邮箱两种登录方式均使用合法的 SQL 条件组合，避免 `Textual SQL expression '' should be explicitly declared as text('')` 异常。
- 退出后重新登录的 CSRF 流程：`/logout` 清除旧 Cookie 后，前端立即请求 `/csrf` 领取新的匿名令牌。所有写请求在缺失 Token 时会先领取；若收到一次 `403 CSRF 校验失败`，自动刷新 Token 并仅重试一次，随后仍失败才向用户提示。该策略覆盖退出后重新登录、刷新页面和 Token 失效场景，且不会对其他 `403` 自动重试。

## 2026-08-03：PostgreSQL 与认证 P0 修复

### 已完成

- PostgreSQL 已使用本地 `.env` 中的 `DATABASE_URL` 完成连接测试；`study_auth` 数据库已执行 Alembic 初始迁移，认证表和索引已创建。
- `requirements.txt` 已加入 `psycopg`；Alembic 不再从 `alembic.ini` 固定读取 SQLite 地址，而是与应用共同从 `.env` 读取连接串。
- 初始迁移已改为明确的建表与索引操作，撤销也只影响本迁移创建的对象，不再调用全局 `create_all/drop_all`。
- 应用启动只会在 `environment=test` 时创建测试表；本地和生产运行都必须先执行 `alembic upgrade head`。
- Refresh Token 使用独立 HMAC 密钥，并增加会话族、行锁、旧 Token 重用检测和整族撤销；前端在读取当前用户遇到一次 401 时会尝试刷新会话并重试。
- 可选的 `TRUSTED_PROXY_IPS` 配置会只对指定反向代理启用转发头解析，避免公网客户端伪造来源 IP。
- 生产配置会拒绝默认 Fernet 加密密钥、默认认证密钥、空 CORS 来源和非 PostgreSQL 数据库地址。

### 迁移与验证命令

```powershell
Set-Location 'D:\前端知识\05_网页前后端程序\后端程序\auth_service'
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python -m alembic upgrade head
python -m alembic current
python -m pytest -q
```

### 本次 P0 提交前待验证

- 执行 `python -m alembic upgrade head`，将现有数据库升级到当前 `0005_login_history`。
- 执行 `python -m pytest -q`，重点确认 Refresh Token 重用会撤销整组会话的回归测试。
- 执行前端 `npm run build`，确认自动刷新会话的请求封装可正常构建。
- 新增独立 Refresh Token HMAC 密钥后，旧版本产生的 Refresh Cookie 会失效，首次部署应要求已有用户重新登录。
- 仅在 Nginx 等可信反向代理的实际来源 IP 已明确时配置 `TRUSTED_PROXY_IPS`；不要填入公网来源或 `*`。

### 已完成与待下一阶段处理（P1）

- Redis 限流已使用原子 Lua 脚本，并在 Redis 不可用时拒绝请求而非降级为单实例限流。
- Outbox 已具备条件领取锁、指数退避和 `dead` 最终失败状态；生产环境仍需用 systemd、Celery 或队列调度器周期执行 Worker。
- PostgreSQL 集成测试、健康检查、结构化日志、监控和备份恢复演练。


## 2026-08-04：登录/注册图片人机验证与 MFA 暂停

### 当前产品行为

- MFA（验证器动态码）已暂时下线：前端不显示“账号安全”入口和登录动态码输入框；后端默认 `MFA_ENABLED=false`，已有 `mfa_totp` 记录保留但不会阻断登录或密码重置。
- 仅登录需要填写图片中的 5 位字符；注册不展示也不校验图片验证码，仍通过邮箱验证码完成账号激活。图片验证码仅用于基本的人机区分，不能替代密码、邮箱验证、CSRF、限流或未来恢复 MFA 的第二因素认证。
- 注册密码规则与后端保持不变：至少 12 位，且包含英文字母、数字和特殊符号。前端只有在密码框已有输入时才展示规则和强度提示；密码为空时隐藏提示。

### 后端接口与校验顺序

1. 前端先请求 `GET /api/auth/captcha`，获得 `captcha_id`、图片 `image` 和 `expires_in`。
2. 仅 `POST /api/auth/login` 提交 `captcha_id`、`captcha_answer`；`POST /api/auth/register` 保持用户名、邮箱和密码字段，不显示图片验证码。
3. 服务端对登录挑战记录执行行锁校验：答案只保存 HMAC，不保存明文；5 分钟过期，成功后立即 `consumed`，连续错误最多 5 次。
4. 图片刷新会取得全新挑战；旧图的答案不能用于新挑战。登录失败后，前端会自动换一张图。
5. 过期挑战会在后续获取图片时清理，避免表无限增长。

### 数据库与配置

- 新增 Alembic `0004_captcha_challenges`，创建 `captcha_challenges`：`id`、答案 HMAC、过期时间、消费时间、尝试次数和创建时间。
- `.env` 未显式配置时也会使用默认值：`MFA_ENABLED=false`、`CAPTCHA_ENABLED=true`。如需临时关闭图片验证码，仅在本地/测试环境设置 `CAPTCHA_ENABLED=false`；生产环境不要关闭。
- 图片验证码仍会受到按 IP 的 `captcha-ip` 限流，防止批量刷新挑战。

### 部署与人工验证

```powershell
Set-Location 'D:\前端知识\05_网页前后端程序\后端程序\auth_service'
.\.venv\Scripts\Activate.ps1
alembic upgrade head
alembic current
uvicorn app.main:app --reload --port 8000
```

`alembic current` 应显示 `0005_login_history`。前端开发服务热更新后，只有登录表单显示图片验证码；注册页面不显示图片验证码，在密码框输入第一个字符后才显示密码规则与强度提示。

## 2026-08-04：异地 / 新 IP 登录检测

### 目标与边界

- 目的：在成功登录后识别“此前未见的 IP”或“此前未见的地区”，向账户邮箱发送安全提醒；它是提醒能力，不是单凭地区自动封禁账户的依据。
- 首次成功登录没有可比较的基线，只写入历史记录，**不发送异地登录告警**。
- 注册邮箱验证码仍正常发送；异地告警与注册验证码是两类不同的邮件。
- 地理定位只能描述公网出口 IP 的大致归属地，不能用于识别某个具体住址或断定操作者身份。

### 成功登录后的执行顺序

```text
POST /api/auth/login 校验成功
  -> 返回 Access / Refresh / CSRF Cookie
  -> 后台任务 process_login_risk
  -> 解析 IP 地区，查询历史 IP 与地区
  -> 追加 login_history
  -> 新 IP 或新地区：写入 email_outbox 并异步发送告警
```

`login_history` 是 PostgreSQL `study_auth` 数据库中的审计表。每条成功登录保存：`user_id`、`ip_hmac`、`country`、`province`、`city`、`user_agent`、`device_hmac`、`anomaly` 和 `login_at`。IP 在历史表中只以 HMAC 保存；告警邮件中的 IP 也会脱敏。

`audit_events` 同时保存登录成功或失败等审计事件；`email_outbox` 保存需要投递的异地登录告警。Redis 配置后使用 `auth:known-locations:{user_id}` 保存 90 天滚动的已知地区集合以加快判断，但 PostgreSQL 历史表才是审计真相来源。

### 中国 IP 离线库与可迁移配置

面向中国用户时，优先使用 `ip2region` 的 `.xdb` 数据文件。中国公网 IP 可返回中文的国家、省、市和运营商信息；GeoLite2 仅作为没有匹配时的全球兜底。

数据文件放在项目内，避免写死 Windows 盘符或服务器绝对路径：

```text
auth_service/
  data/
    ip2region_v4.xdb
    ip2region_v6.xdb       # 可选，仅在需要 IPv6 定位时放置
```

`.env` 配置相对于 `auth_service` 根目录，服务无论从哪个工作目录启动都会按项目目录解析：

```env
IP2REGION_V4_DATABASE_PATH=data/ip2region_v4.xdb
IP2REGION_V6_DATABASE_PATH=data/ip2region_v6.xdb
GEOIP_DATABASE_PATH=data/GeoLite2-City.mmdb
LOGIN_LOCATION_TTL_DAYS=90
```

不要写成 `./后端程序/auth_service/data/ip2region_v4.xdb`。该路径已经处于 `auth_service` 内时会重复拼接目录，导致找不到文件。未配置或文件缺失时，系统会安全降级为只比较新 IP，不会阻断登录。

### 为什么本地测试显示 Reserved / 127.0.0.1

本地前端访问 `http://127.0.0.1:8000` 时，后端收到的来源就是 `127.0.0.1`；它是回环地址，不是电脑的公网 IP。`127.0.0.1`、`192.168.x.x`、`10.x.x.x` 等本地/内网地址没有可用的省市归属，离线库返回 `Reserved` 属于正常现象。

若要验证数据文件本身，可用已知公网 IP 调用定位函数；例如 `113.118.113.77` 应得到 `中国 / 广东省 / 深圳市`。这只是库验证，不等同于完整的真实来源 IP 链路验证。

### 生产环境真实 IP 链路与安全配置

推荐部署链路：

```text
用户公网 IP -> Nginx / 负载均衡 -> FastAPI (127.0.0.1:8000)
```

Nginx 应把客户端地址传给后端：

```nginx
location / {
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_pass http://127.0.0.1:8000;
}
```

同机 Nginx 代理时，应用配置：

```env
TRUSTED_PROXY_IPS=127.0.0.1
```

若 Nginx 在独立机器或负载均衡之后，填写实际代理的内网 IP；不要填写 `*`、公网来源地址或任意客户端 IP。只有在 FastAPI 的 `8000` 端口不直接暴露给公网、所有请求都经由该可信代理时，才可以信任转发头。否则攻击者可伪造 `X-Forwarded-For`，影响异地登录判断和限流。

完整人工验收应部署到具备公网入口的测试服务器，经 Nginx 访问，并使用手机流量等不同网络分别登录：首次登录不发告警；随后从不同公网出口登录应生成一条 `anomaly=true` 的历史记录和一封异地登录提醒邮件。

## 2026-08-04：HIBP 泄露密码检查与密码历史

### 目标

补上此前审查结论中的两个 P2 项：注册/改密时对接公开泄露密码库（HaveIBeenPwned Pwned Passwords，k-anonymity 模式），以及禁止重复使用最近使用过的密码。

### 泄露密码检查（HIBP k-anonymity）

- 新增 `app/pwned.py`：对密码计算 SHA-1，只把摘要前 5 位发送到 `https://api.pwnedpasswords.com/range/{prefix}`，剩余后缀在本地比对出现次数。完整密码与完整摘要均不离开本服务；前缀响应内存缓存 24 小时，避免重复请求。
- 检查点：`POST /api/auth/register`、`POST /api/auth/password-reset/confirm`、新增的 `POST /api/auth/password-change`。命中泄露密码返回 400：`该密码已在公开泄露数据库中出现过 N 次，请更换一个未泄露的密码。`
- 可用性策略：默认 fail-open——泄露库不可达时记警告日志并放行（`PWNED_CHECK_STRICT=false`）；`PWNED_CHECK_STRICT=true` 时改为 fail-closed（503）。国内网络访问 api.pwnedpasswords.com 不稳定时，可把 `PWNED_API_BASE_URL` 指向自建镜像/代理（保持 `/range/{prefix}` 接口格式）。
- 前端注册页（`AuthView.vue`）在密码满足基本规则后做一次同接口的非阻断性提示（`src/services/pwned.js`）：命中时显示“该密码已在公开泄露数据库中出现过 N 次，建议更换”。前端检查失败静默跳过，不会阻止提交；是否拒绝以服务端为准。

### 密码历史

- 新增 `password_history` 表（Alembic `0006_password_history`）与 `app/password_history.py`：被替换的旧密码以 Argon2id 哈希归档，每用户保留最近 `PASSWORD_HISTORY_COUNT`（默认 5）条，超出自动裁剪。
- 新密码不得与当前密码或任意归档哈希相同，否则返回 400：`新密码不能与当前或最近使用过的密码相同。`
- 写入时机：密码重置成功、修改密码成功时归档被替换的旧密码。
- 新增 `POST /api/auth/password-change`（登录 + CSRF + 当前密码校验）：成功后保留当前会话，撤销该用户其余全部会话（`revocation_reason=password_change`），并清零失败计数与锁定状态；前端新增 `/security` 账号安全页（`SecurityView.vue`），提供“修改密码”表单与 MFA 开关区（MFA 区块仅在 `/me` 返回 `mfa_enabled=true` 时显示）。

### 配置（.env）

```
PWNED_CHECK_ENABLED=true
PWNED_CHECK_STRICT=false
PWNED_API_BASE_URL=https://api.pwnedpasswords.com
PWNED_TIMEOUT_SECONDS=3.0
PASSWORD_HISTORY_COUNT=5
```

### 验证

- 后端：`python -m pytest -q`（22 个用例全过，新增 5 个：SHA-1 格式、策略 fail-open/fail-closed、注册拒绝泄露密码、重置拒绝历史密码、改密撤销其他会话）。
- 前端：`npm run build` 通过。
- 升级现有数据库：`alembic upgrade head`（新增 `0006_password_history`）。


# 变更记录：HIBP 泄露密码检查与密码历史

- **日期**：2026-08-04
- **范围**：登录注册模块（前端 study-blog-vue + 后端 auth_service）
- **目标**：补上审查结论中的两个 P2 项——
  1. 对接公开泄露密码库（HaveIBeenPwned Pwned Passwords，k-anonymity 模式）
  2. 禁止重复使用最近使用过的密码（密码历史）
  附带新增「登录后修改密码」能力，使密码历史真正生效。

---

## 一、后端改动（D:\前端知识\05_网页前后端程序\后端程序\auth_service）

### 1. app/config.py（修改）
新增 6 个配置项：

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `pwned_check_enabled` | `true` | 是否启用泄露密码检查 |
| `pwned_check_strict` | `false` | `false`=泄露库不可达时放行并记日志（fail-open）；`true`=拒绝注册/改密返回 503（fail-closed） |
| `pwned_api_base_url` | `https://api.pwnedpasswords.com` | HIBP 接口地址，国内可指向自建镜像（保持 `/range/{prefix}` 格式） |
| `pwned_timeout_seconds` | `3.0` | 外部请求超时 |
| `password_history_count` | `5` | 每用户保留的最近密码数量 |

### 2. app/models.py（修改）
新增 `PasswordHistory` 模型（表 `password_history`）：
- `id`、`user_id`（外键，级联删除）、`password_hash`（Argon2id）、`created_at`

### 3. app/pwned.py（新增文件）
HIBP k-anonymity 检查客户端：
- `sha1_hex()`：密码 SHA-1 摘要（大写）
- `_fetch_range()`：只发送摘要**前 5 位**到 `/range/{prefix}`，后缀在本地比对；响应内存缓存 24 小时
- `breach_count()`：返回泄露出现次数；数据库不可达时返回 `None`
- `enforce_breach_policy()`：策略执行——命中泄露返回 400（含次数）；严格模式不可达返回 503；默认 fail-open

### 4. app/password_history.py（新增文件）
- `password_was_used_before()`：候选密码与当前哈希 + 全部归档哈希比对（Argon2id verify）
- `archive_password()`：归档被替换的旧密码哈希，并按 `keep` 数量裁剪超出部分

### 5. app/schemas.py（修改）
- 新增 `PasswordChangeRequest`（`current_password` + `new_password`，新密码复用强度校验）
- `UserResponse` 新增 `mfa_enabled` 字段

### 6. app/routers/auth_secure.py（修改）
- `POST /register`：限流之后增加 `enforce_breach_policy(settings, payload.password)`，命中泄露密码直接拒绝，不创建账号
- `POST /password-reset/confirm`：验证码有效后增加——密码历史检查（命中返回 400「新密码不能与当前或最近使用过的密码相同」）→ 泄露检查 → 归档旧密码 → 才设置新密码；历史/泄露拒绝**不消耗**重置验证码，可换密码重试
- **新增** `POST /password-change`（登录态改密）：
  - 需 CSRF + 当前密码校验
  - 限流：按 IP 10 次/15 分钟、按用户 5 次/15 分钟
  - 密码历史检查 + 泄露检查
  - 成功后**保留当前会话，撤销该用户其余全部会话**（`revocation_reason=password_change`）
  - 清零失败计数与锁定状态，归档旧密码
- `GET /me`：改为返回 `mfa_enabled`（由用户是否启用 MFA 决定）

### 7. alembic/versions/0006_password_history.py（新增迁移）
创建 `password_history` 表 + `user_id` 索引；`down_revision = 0005_login_history`。

### 8. tests/test_auth_flow.py（修改）
- 测试夹具 `client()` 增加 `pwned_check_enabled=False`，保持测试离线、不依赖外部 API
- 新增 5 个用例：
  1. `test_sha1_hex_matches_hibp_k_anonymity_format`——SHA-1 格式（"password" 的摘要断言）
  2. `test_breach_policy_rejects_or_fails_open`——400 拒绝 / fail-open 放行 / 严格模式 503
  3. `test_register_rejects_breached_password`——注册接口拒绝泄露密码，正常密码通过
  4. `test_password_reset_rejects_recently_used_passwords`——重置时旧密码被拒，且同一验证码可换新密码重试
  5. `test_password_change_requires_current_password_and_revokes_other_sessions`——错误当前密码被拒、历史密码被拒、其他会话被撤销、当前会话保留、旧密码登录失效

### 9. 配置与文档（修改）
- `.env.example`：新增 5 个变量的注释说明
- `README.md`：新增「泄露密码检查与密码历史」章节
- `SECURITY.md`：补充相应安全说明

---

## 二、前端改动（D:\前端知识\05_网页前后端程序\前端程序\study-blog-vue）

### 1. src/services/auth.js（修改）
新增 `changePassword(payload)` API 封装（调用 `POST /api/auth/password-change`）。

### 2. src/services/pwned.js（新增文件）
浏览器端 k-anonymity 检查：
- WebCrypto SHA-1，只发送前 5 位前缀到 `api.pwnedpasswords.com/range/`
- 5 秒超时；任何失败静默返回 `null`，**不阻断表单**（是否拒绝以服务端为准）

### 3. src/views/AuthView.vue（修改）
- 引入 `watch` 与 `pwnedBreachCount`
- 密码输入防抖 600ms 后做泄露检查；命中时在规则清单下方显示：
  「该密码已在公开泄露数据库中出现过 N 次，建议更换。」

### 4. src/views/SecurityView.vue（重写）
账号安全页（路由 `/security`）：
- 「登录密码」卡片：当前密码 + 新密码 + 确认密码，实时规则清单，提交 `changePassword`
- 「动态验证（MFA）」卡片：仅当 `/me` 返回 `mfa_enabled=true` 时显示（当前后端 MFA 关闭，故默认隐藏）

### 5. src/router/index.js（修改）
- 新增路由：`{ path: "/security", name: "security", component: SecurityView, meta: { requiresAuth: true } }`
- **修复**：补充 `import SecurityView from "../views/SecurityView.vue"`（初次提交遗漏，导致模块加载时 `SecurityView is not defined`、应用白屏；该问题已由本次记录一并修复）

### 6. src/App.vue（修改）
登录后顶部导航新增「安全设置」链接（指向 `/security`）。

---

## 三、数据库变更

```powershell
cd D:\前端知识\05_网页前后端程序\后端程序\auth_service
.\.venv\Scripts\python.exe -m alembic upgrade head   # 已执行，当前 0006_password_history (head)
```

---

## 四、验证结果

| 检查项 | 结果 |
|---|---|
| 后端自动化测试 `python -m pytest -q` | ✅ 22 passed（原 17 + 新增 5） |
| 前端生产构建 `npm run build` | ✅ 通过（46 modules） |
| dev 服务器响应（前端 `/`、后端 `/api/auth/csrf`、代理链路） | ✅ 全部 200 |
| 数据库迁移 `alembic current` | ✅ 0006_password_history (head) |

---

## 五、上线注意事项

1. **国内网络**访问 `api.pwnedpasswords.com` 可能不稳定：默认 fail-open 不影响可用性；若需严格拒绝，设 `PWNED_CHECK_STRICT=true` 或把 `PWNED_API_BASE_URL` 指向自建镜像。
2. 部署需在 `.env` 中显式确认新配置项（不配置则走默认值）。
3. 新表 `password_history` 只增不改，`archive_password` 自动裁剪，无手动维护需求。
