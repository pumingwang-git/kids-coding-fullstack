# Docker 构建上下文排除：临时/缓存/虚拟环境/运行期数据。
# 缺了它，Docker 会尝试读取 .codex-pytest-* / .pt-* 临时目录，撞上 Windows
# 文件锁报 "Access is denied"，且上下文巨大。
.git
.gitignore
.venv
venv
env
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
.mypy_cache
# codex / pytest 临时工作目录（.codex-pytest-*、.pt-*、.pt2-*、.pt3-* 等）
.codex-*
.pt-*
.pt2-*
.pt3-*
.tmp-*
# 运行期数据（上传/转码/缓存），容器内由 Dockerfile 重新建
data
# 本地密钥与示例（绝不进镜像）
.env
.env.*
# 前端/依赖
node_modules
# 日志与打包产物
*.log
*.tar
*.zip
# 临时脚本
解密滑块-临时.py
_probe_cors.py
