# 白名单式：先排除一切，再只放行应用运行/迁移必需的文件。
# 原因：auth_service 根目录散落大量 pytest/codex 临时工作目录
# （.pytest-audit-*、.codex-pytest-*、.pt-*、.pt2-* …），命名各异且部分被
# Windows 文件锁占用，构建时逐个排除排不尽，会撞 "Access is denied"。
# 白名单一次性绕开全部，也让构建上下文最小。

# 排除一切
*

# 放行运行/迁移必需
!app/
!alembic/
!alembic.ini
!requirements.txt

# 放行目录内再剔除缓存（后置规则覆盖前面的放行）
**/__pycache__/
**/*.pyc
**/*.pyo
